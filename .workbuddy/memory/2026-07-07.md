# 2026-07-07

## 隐藏前台 "任务 Profile" nav tab
- **文件**: `frontend/src/App.vue`
- NAV_ITEMS: 注释掉 profiles 条目
- Profiles section: `v-if="false && navTab === 'profiles'"`（隐藏不删除）

## Codex 模型目录：上下堆叠
- **文件**: `frontend/src/App.vue`
- `grid grid-cols-2` → `space-y-ax-sm`（两个输入框上下排列）

## AxModelSelect 多选固定高度 + textarea 输入
- **文件**: `frontend/src/components/ui/AxModelSelect.vue`
- 新增 `MULTI_SIZE_CLASSES`（不带 `h-7` 固定高度，只有 padding + text）
- 多选 trigger: **固定 `h-[140px]` + `flex-col` + `overflow-hidden`**，开/关高度不变
- open 态: 标签区域 `max-h-[60px] overflow-y-auto`（顶部有限高度）+ textarea `flex-1` 占满剩余空间
- 多选输入改为 `<textarea>` 支持多行，无框线/无背景样式，Enter 提交、Shift+Enter 换行
- closed/open 态拆分为 `v-if="multiple"` 分支，单选逻辑不受影响

## Claude 模型映射删除 Small/Fast
- **文件**: `frontend/src/App.vue`
- 删除 `formClaudeSmall` ref、加载、保存及模板 Small/Fast 下拉框
- `grid-cols-5` → `grid-cols-4`

## 下拉框样式优化
- **文件**: `frontend/src/components/ui/AxModelSelect.vue`
  - 设置 `menu-width="200px"`（最小宽度）
  - 空状态加 `px-3`、图标、提示文字，改善左右无 padding 的拥挤感
- **文件**: `frontend/src/components/ui/AxSelect.vue`
  - `dropdownWidth` 默认值 `'auto'` → `'200px'`（所有 AxSelect 下拉框默认最小宽度 200px）
  - `dropdownMaxWidth` 默认值 `'300px'` → `'320px'`
  - 空状态加 `px-3`
