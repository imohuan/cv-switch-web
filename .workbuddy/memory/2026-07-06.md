# 2026-07-06

## 多平台启动命令功能
- **后端** `backend/src/services/profiles.ts`: 新增 `launchCommands()` 函数，返回 `{bash, powershell, cmd}` 三种平台格式。原 `launchCommand()` 保留作为向后兼容（返回 bash 格式）。新增 `LaunchCommands` 和 `CommandPlatform` 类型导出。
- **后端** `backend/src/routes/providers.ts`: `profileResponse()` 和 GET `/api/profiles` 接口新增 `commands` 字段。
- **前端** `frontend/src/api.ts`: Profile 接口新增 `commands: {bash, powershell, cmd}` 字段。
- **前端** `frontend/src/App.vue`: 启动命令区域从单行代码改为带 Tab 切换的多平台面板，支持 Bash/PowerShell/CMD 三种格式切换和复制。新增 `profileCommandPlatform` 状态管理每个 Profile 的选中平台。

## Provider 选择后自动回填模型
- **前端** `frontend/src/App.vue`: 新增 `watch(profileProviderId, ...)` 监听，切换 Provider 时自动从 `extra_config.{claude,codex}` 回填默认模型、Haiku/Sonnet/Opus 模型、Codex 模型目录到表单字段。
- 之前创建 Profile 时 `extra_config` 为 `{"claude":{}}`（空），因为表单字段从未自动填充。修复后选择 Provider 即可看到预配置的模型值。
## 修复 Claude Profile 双重 Auth 冲突
- **后端** `backend/src/services/claude.ts`（全局配置）: 移除 `settings.env.ANTHROPIC_AUTH_TOKEN` 写入，改用 `delete` 清理旧值。
- **后端** `backend/src/services/profiles.ts` 的 `writeClaudeProfile()`（Profile 配置）: 同样移除 `ANTHROPIC_AUTH_TOKEN`。之前只修了全局的，遗漏了 Profile 专用的写入函数，导致「重新写入配置」仍然输出 AUTH_TOKEN。
## Claude 二进制自动复制
- **后端** `backend/src/services/profiles.ts`: 新增 `copyClaudeBinary()` 函数，写 Claude Profile 时自动从全局 `~/.local/bin/claude.exe` 复制到 Profile 的 `.local/bin/claude.exe`，避免每个 Profile 都需要手动跑 `claude install`。
## 后台日志系统
- **后端** `backend/src/services/logger.ts`: 统一日志模块，控制台 + 文件双输出（路径: `~/.cc-switch-web/logs/cc-switch-YYYY-MM-DD.log`），按天轮转。提供 `claudeProxy` / `codexProxy` / `models` / `profiles` 四个子模块标签。
- **后端** `backend/src/routes/claudeProxy.ts`: `handleClaudeMessages` 集成日志——记录请求开始、上游请求 URL/Model、响应成功（含延迟 ms）或失败（含上游错误 body 预览前 500 字符）。
- **后端** `backend/src/routes/codexProxy.ts`: `handleCodexResponses` 同样集成日志。
- **后端** `backend/src/routes/providers.ts`: 模型列表获取端点集成日志；新增 GET `/api/logs` 端点返回最近 200 行日志（供前端查看）。
- **后端** `backend/src/routes/providers.ts`: 修复 `require('fs')` / `require('path')` 为 ESM `import`。

## 根因：全局 settings.json 覆盖 Profile 配置
- **根因**: Claude Code 启动时 merge 全局 `~/.claude/settings.json` 和 Profile 的 `HOME/.claude/settings.json`，全局的 `env` 块优先级更高。全局配置中残留了 `ANTHROPIC_BASE_URL: http://newapi.imohuan.shop/v1` 等旧值，导致 Claude Code 直接请求 New API 而非走本地 Proxy。
- **修复**: 清理全局 `~/.claude/settings.json`，移除所有 `ANTHROPIC_*` 环境变量，仅保留 `skipDangerousModePermissionPrompt` 和 `theme`。
- **后端** `backend/src/index.ts`: 请求日志中间件从仅记录 `/proxy/` 改为全量记录所有请求（含 host、user-agent、状态码、耗时）。
- **后端** `backend/src/services/providerConfig.ts`: `publicBaseUrl()` 默认端口从 `3001` 改为 `3120`，与 `index.ts` 一致。
- **后端** `backend/src/services/profiles.ts` + `claude.ts`: ANTHROPIC_MODEL 等模型名改为 `claude-sonnet-4-20250514`，避免 Claude Code 客户端白名单校验拦截。实际模型映射由 Proxy 内部用 `provider.model` 完成。
- **后端** `backend/src/services/profiles.ts` + `claude.ts`: ANTHROPIC_API_KEY 从 `"PROXY_MANAGED"` 改为使用 **provider 真实 API key**。Claude Code 的 `~/.claude.json` 中有已批准的 key 列表（`customApiKeyResponses.approbed`），只有已批准的 key 才能绕过 `/login` 检查。之前用假 key 导致 Claude Code 判定"未登录"。
- **根因**: Claude Code 启动时会检查当前 `ANTHROPIC_API_KEY` 是否在已批准列表中（通过 key 尾缀匹配）。不在列表中 → 显示 "Not logged in · Please run /login"，**不会发出任何 HTTP 请求**到 BASE_URL。
- **修复方案**: Profile 配置直接写入 Provider 的真实 API key（与全局已批准的 key 一致），Proxy 仍然用数据库中的 key 发请求给上游。
- **重要补充**: Claude Code 的认证状态（`numStartups`、`customApiKeyResponses`、`projects`）存储在 `~/.claude.json`（**注意不在** `.claude/` 子目录内）。当用户用 `$env:HOME` 切换到 Profile 目录后，Claude Code 在新 HOME 下找不到这个文件也会判定 "Not logged in"，**同样不会发任何 HTTP 请求**。
- **修复**: 后端 `backend/src/services/profiles.ts` 新增 `copyClaudeAuthState()` 函数，在写 Claude Profile 时自动从全局 `~/.claude.json` 复制到 Profile 的 `~/.claude.json`。以后创建新 Profile 不再需要手动操作。
- **Windows 上 HOME 无效**: Claude Code 在 Windows 上不读取 `HOME` 环境变量，而是使用 `USERPROFILE`（通过 `os.homedir()`）。`CLAUDE_CONFIG_DIR` 虽然可以覆盖配置目录，但会导致找不到凭证。最终方案：**不用 HOME 也不用 CLAUDE_CONFIG_DIR**，直接在命令行设置 `ANTHROPIC_BASE_URL` / `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` 等环境变量。Claude Code 使用全局凭证认证，API 请求走 Proxy。
- **Express body parser 限制**: 默认 100kb，Claude Code 请求含大量 system prompt + tools 定义，请求体可达数 MB。改为 `express.json({ limit: '50mb' })`。
- **HEAD 探测请求**: Claude Code 启动时发 HEAD 请求到 BASE_URL 探测服务器存活。Proxy 需要处理 HEAD 请求返回 200，否则 Claude Code 判定连接失败。

## 模型角色映射（Haiku/Sonnet/Opus）
- **问题**: 之前所有环境变量都设为同一个 `claude-sonnet-4-20250514`，Proxy 用 `provider.model` 发请求，无法区分角色。Provider 配置的 Haiku/Sonnet/Opus 模型完全被忽略。
- **修复**: 不同角色用不同的 Claude 模型名（都在白名单内），Proxy 根据名称映射到 Provider 实际模型：
  - `ANTHROPIC_MODEL` / `ANTHROPIC_DEFAULT_SONNET_MODEL` → `claude-sonnet-4-20250514` → Proxy 映射到 `extra_config.claude.sonnetModel`
  - `ANTHROPIC_DEFAULT_HAIKU_MODEL` / `ANTHROPIC_SMALL_FAST_MODEL` → `claude-haiku-4-5-20251001` → Proxy 映射到 `extra_config.claude.haikuModel`
  - `ANTHROPIC_DEFAULT_OPUS_MODEL` → `claude-opus-4-8-20250610` → Proxy 映射到 `extra_config.claude.opusModel`
- **涉及文件**: `backend/src/routes/claudeProxy.ts` 新增 `mapClaudeModelToProvider()` 函数；`backend/src/services/profiles.ts` 和 `backend/src/services/claude.ts` 更新模型名设置。

---

## Codex model_catalog schema 修复 + env_key 问题

- **问题 1**: Codex 报 `missing field slug` → `missing field display_name` → `missing field supported_reasoning_levels` 等连环 parse error。根因：`codexModelCatalog()` 生成的 JSON 缺少大量 ModelInfo struct 的必填字段（无 `#[serde(default)]` 的字段缺失即报错）。
- **问题 2**: 旧代码字段名 `parallel_tool_calls` 是错的，serde 当未知字段忽略，导致 `supports_parallel_tool_calls` 缺失。正确字段名是 `supports_parallel_tool_calls`。
- **问题 3**: `config.toml` 中 `env_key = "OPENAI_API_KEY"` 导致 Codex 从环境变量读 key，终端启动时未设环境变量报 `Missing environment variable: OPENAI_API_KEY`。
- **修复**:
  - 新建 `backend/src/services/codexCatalog.ts`：完整 TypeScript 类型定义 + 详细注释，镜像 Rust `ModelInfo` struct 的 serde schema。导出 `generateCodexModelCatalog(provider)` 函数。
  - `codex.ts` 和 `profiles.ts`：删除旧的内联 `codexModelCatalog()` 函数，改用 `import { generateCodexModelCatalog } from './codexCatalog.js'`。
  - `codex.ts` 和 `profiles.ts`：`env_key` → `api_key`（Proxy 模式写 `PROXY_MANAGED`，直连模式写真实 key），加 `requires_openai_auth = false` + `supports_websockets = false`。
  - 全局 `~/.codex/config.toml`：`custom` provider 同样从 `env_key` 改为 `api_key`。
  - 全局 `~/.codex/cc-switch-web-model-catalog.json`：更新为完整 schema。
- **必填字段清单**（14 个无 serde default）: slug, display_name, description, supported_reasoning_levels, shell_type, visibility, supported_in_api, priority, base_instructions, supports_reasoning_summaries, support_verbosity, default_verbosity(null), apply_patch_tool_type(null), truncation_policy, supports_parallel_tool_calls, experimental_supported_tools, availability_nux(null), upgrade(null)。
- **Rust 源码参考**: https://github.com/openai/codex/blob/main/codex-rs/protocol/src/openai_models.rs

---

## 前端 UI 优化 — 概览页改为 Tab 布局 + 全局配置查看

- **需求**: 概览页从 4 卡片网格改为 Tab 切换布局，每个 App（Codex/Claude/Gemini/OpenCode）一个 Tab，支持连接/断开 Provider + 查看本地配置文件（与 Profile 折叠面板效果一致）。
- **后端**: `backend/src/routes/providers.ts` 新增 `GET /api/app/:appType/config` 端点，读取全局 `~/.codex/`、`~/.claude/` 等目录下的配置文件（与 Profile config 端点逻辑一致，仅 home_dir 改为 `os.homedir()`）。
- **前端 API**: `frontend/src/api.ts` 新增 `getAppConfig(appType)` 方法。
- **前端 UI**: `frontend/src/App.vue` 概览 section 重写：
  - 顶部 Tab Bar（4 个 App，带连接状态圆点指示器）
  - 每个 Tab 内容区：连接状态卡片（Provider/Model/Base URL 三栏）+ 切换/断开按钮 + 查看本地配置折叠面板（chevron_right 图标旋转，文件列表可逐个展开）
  - 新增状态变量：`statusAppTab`、`expandedAppConfig`、`appConfigs`、`loadingAppConfig`、`expandedAppFiles`
- **顺带修复**: `tsconfig.node.json` 的 `noEmit` → `composite: true`（修复 vue-tsc -b 报错）；AxSelect 缺少 xl 尺寸；AxJsonViewer v-for 类型断言；App.vue 中 COMMAND_PLATFORMS.find 回调变量名遮蔽 bug。
- **JSON 渲染优化**: 配置查看面板（Profile + App）中 `.json` 文件改用 `AxJsonViewer` 组件渲染（可折叠 JSON 树，`expand-level=2` 默认展开 2 层），非 JSON 文件（.toml/.env）仍用 `<pre>` 纯文本。新增 `tryParseJson()` 辅助函数 + `ConfigFile` 类型，加载配置时预解析 `parsed` 字段。
- **Codex Proxy 模型覆盖 bug 修复**: `codexProxy.ts` 的 `responsesToChat()` 中 `const model = provider.model || body.model` 优先用了 Provider 默认模型，导致用户在 Codex CLI 选的模型（如 deepseek-v4-pro）被覆盖成 Provider 的 model 字段（如 gpt-5.5）。修复为 `body.model || provider.model`，Codex 请求的模型优先，Provider 默认模型仅兜底。同时增加模型映射日志（原始模型 ≠ 实际模型时记录映射关系）。
